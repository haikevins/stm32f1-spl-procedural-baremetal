/*
 * This project links without a hosted C runtime and does not require syscalls.
 * Keep this file as an extension point for future UART-backed _write(),
 * heap-backed _sbrk(), and related integrations.
 */
